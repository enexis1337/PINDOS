[package]
name = "dealduck"
version = "0.1.0"
edition = "2021"

[dependencies]
nom = "7"
bitflags = "2"
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"

[features]
default = ["std"]
std = ["serde/std"]
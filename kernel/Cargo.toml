[package]
name = "hammam"
version = "0.2.0"
edition = "2021"
autobins = false

[lib]
name = "hammam"
crate-type = ["staticlib"]
path = "src/main.rs"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = "s"
lto = true
